
{
  "spec": {
    "template": {
      "spec": {
        "containers": [
          - "name": "helm-operator"
            "args": [
              - --enabled-helm-versions=v3
            ]
        ]
      }
    }
  }
}
